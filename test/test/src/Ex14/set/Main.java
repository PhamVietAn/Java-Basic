package Ex14.set;

public class Main {
    public static void main(String[] args) {
        // set kế thừa COLLECTION
        // set không lưu trữ phần tử trùng lặp

        // 4 lơpd triển khai:
        // 1. HashSet: sử dụng bảng băm, không đảm bảo thứ tự
        // 2. LinkedHashSet: sử dụng bảng băm + danh sách liên kết, duy trì thứ tự chèn
        // 3. TreeSet: sử dụng cây đỏ-đen, lưu trữ phần tử theo thứ tự tự nhiên
        // 4. EnumSet: chuyên dụng cho enum, hiệu quả cao
    }
}
